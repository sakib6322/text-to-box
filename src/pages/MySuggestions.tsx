import Suggestions from "./Suggestions";

/** User-facing suggestions (study/practice). Visible to both users and admins. */
export default function MySuggestions() {
  return <Suggestions mode="user" />;
}

